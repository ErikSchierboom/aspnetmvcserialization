<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="~/Content/Site.css" />
</head>
<body>
    <ul>
        <li>
            @Html.ActionLink("Dictionary", "Index", "Dictionary")
            @Html.ActionLink("Enumerable", "Index", "Enumerable")
            @Html.ActionLink("Collection", "Index", "Collection")
            @Html.ActionLink("List", "Index", "List")
        </li>
    </ul>

    @RenderBody()
</body>
</html>
